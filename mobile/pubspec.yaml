name: mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Navegación
  go_router: ^17.5.0

  # Manejo de estado
  flutter_riverpod: ^3.4.2

  # Peticiones HTTP hacia Django REST API
  http: ^1.3.0

  # Guardar JWT (access y refresh token)
  shared_preferences: ^2.5.3


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true

  assets:
    - web/favicon.png